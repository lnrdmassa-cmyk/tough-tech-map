# Tough Tech Map · Europe — environment
#
# Copy this file to ".env.local" and paste your Supabase project values.
# Leave BOTH blank to run in DEMO MODE: the app uses the bundled 46-facility
# seed and the map works with no backend.
#
# Find these in Supabase: Project Settings -> API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
